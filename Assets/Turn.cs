using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Turn : MonoBehaviour {

// some resources are turn only
// other resources accumulated over the game
// so how does it work?
// playing a card has some effect on resources
// the resource needs to understand where it lives
// the resource can inherit it's turnness or... otherwise?

    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
